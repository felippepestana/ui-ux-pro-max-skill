import { Users2 } from "lucide-react";
import { PlaceholderPage } from "@/components/app-shell/placeholder-page";

export default function SquadsPage() {
  return (
    <PlaceholderPage
      phase="Fase 2 · Squads"
      title="Catálogo de squads"
      description="Pipelines de agentes IA por área do direito (jurisprudência, petições, due diligence)."
      icon={Users2}
    />
  );
}
