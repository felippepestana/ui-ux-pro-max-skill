import { UserSquare2 } from "lucide-react";
import { PlaceholderPage } from "@/components/app-shell/placeholder-page";

export default function ClientsPage() {
  return (
    <PlaceholderPage
      phase="Fase 4 · Clientes"
      title="Clientes"
      description="Cadastro, contratos, histórico de casos e canal compartilhado por cliente."
      icon={UserSquare2}
    />
  );
}
