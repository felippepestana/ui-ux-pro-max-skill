import { Sparkles } from "lucide-react";
import { PlaceholderPage } from "@/components/app-shell/placeholder-page";

export default function AgentsPage() {
  return (
    <PlaceholderPage
      phase="Fase 2 · Agentes"
      title="Agentes"
      description="Perfil, skills, custos e auditoria de cada agente IA do escritório."
      icon={Sparkles}
    />
  );
}
