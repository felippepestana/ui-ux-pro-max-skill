name: Claude Code Review (Optional)

# Este workflow é opcional e requer configuração especial da Anthropic
# Desabilitado por padrão. Para ativar:
# 1. Configure CLAUDE_CODE_OAUTH_TOKEN nos GitHub Secrets
# 2. Remova "if: false" abaixo

on:
  pull_request:
    types: [opened, synchronize, ready_for_review, reopened]

jobs:
  claude-review:
    # Disabled: This workflow requires special Anthropic setup
    # The anthropics/claude-code-action may require additional permissions or configuration
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "Claude Code Review disabled - use GitHub's native tools or configure proper Anthropic integration"
